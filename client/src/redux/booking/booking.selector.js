// import { createSelector } from 'reselect';
// // const selectGuest = state => state.guest;
// const selectAdult = state => state.adult;
// // export const selectCollection = createSelector(
// //   [selectPeople],
// //   booking => booking.guest
// // );

// export const selectGuestCount = createSelector(
//   // selectGuest,
//   selectAdult,
//   (selectGuest, selectAdult) => ({ total: selectAdult })
//   // guests =>
//   // guests.reduce((acc, guest) => acc + guest, 0)
// );
// console.log(selectGuestCount());
